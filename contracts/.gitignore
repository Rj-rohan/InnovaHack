# Node modules
/node_modules

# Compilation output
/dist

# Hardhat build artifacts
/artifacts
/cache
/types

# Environment files
.env
.env.*
!.env.example

# Local dev chain deployment — regenerated on every deploy:local, machine-specific.
/deployments/31337.json

# Public network deployments (Sepolia etc.) are committed — they are shared and stable.

