# Node modules
/node_modules

# Compilation output
/dist

# Hardhat build artifacts
/artifacts
/cache
/types

# Environment files
.env
.env.*
!.env.example

# Deployment records ARE committed — client/ and server/ read addresses and ABIs from them,
# and 31337.json is what a fresh clone needs to run the local demo without redeploying.

